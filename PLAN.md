# 엑셀 섯다 게임 제작 계획

목표: 매크로 포함 단일 파일(`섯다.xlsm`). 플레이어 vs AI 1:1, 2장 섯다, 게임 포인트 전용 오락용.

---

## 1. 게임 규칙 범위

### v1 (첫 완성 목표)
- 화투 20장 (1~10월 각 2장)
- 각자 2장 배분, 베팅 라운드 1회, 쇼다운
- 족보: 38광땡 > 18광땡 > 13광땡 > 장땡~1땡 > 알리 > 독사 > 구삥 > 장삥 > 장사 > 세륙 > 갑오~끗수 > 망통
- 베팅: **콜 · 하프 · 다이** (자금 부족 시 올인 처리)
- 무승부(같은 족보) 시 판돈 이월(재경기) 또는 반분 — 기본: 재경기

### v2 (구현 완료)
- ✅ 따당 베팅 (직전 레이즈의 2배, 레이즈가 있어야 가능)
- ✅ 구사/멍텅구리구사 (알리 이하/장땡 이하 자동 재경기)
- ✅ 암행어사 (4·7 열끗 → 13·18광땡 잡기)
- ✅ 땡잡이 (3·7 → 1~9땡 잡기)
- ✅ 난이도 선택 UI (v1에 포함), 인원 선택 2~5인, 우측 족보 순위 패널

---

## 2. 파일 구조

### 시트 (4개)
| 시트 | 용도 | 표시 |
|---|---|---|
| `Game` | 게임판. 격자/머리글 제거, 배경색, 도형 기반 UI | 표시 |
| `Assets` | 화투패 20장 + 뒷면 도형 원본 (복사해서 사용) | 숨김 |
| `Data` | 족보 랭크 테이블, AI 성향 파라미터, 설정값 | 숨김 |
| `Save` | 게임 상태·자금·기록 저장 슬롯 | 숨김 |

### VBA 모듈 (8개)
| 모듈 | 역할 |
|---|---|
| `modMain` | 게임 루프 상태 머신 (IDLE→ANTE→DEAL→BET→SHOWDOWN→SETTLE) |
| `modDeck` | 덱 생성, Fisher-Yates 셔플, 배분 |
| `modHand` | 족보 판정 → 정수 랭크 반환 (클수록 강함) |
| `modBetting` | 콜/하프/다이 처리, 판돈·자금 계산 |
| `modAI` | AI 의사결정 (아래 4절) |
| `modUI` | 패 렌더링, 애니메이션, 버튼 표시/숨김, 메시지 |
| `modSound` | 효과음 (WinAPI `PlaySound`, 64비트 `PtrSafe` 선언) |
| `modSave` | 저장/이어하기 (Save 시트 직렬화) |
| `clsGameState` | 현재 판 상태 클래스 (패, 판돈, 턴, 자금, 기록) |

---

## 3. 핵심 설계

### 카드 표현
- 카드 = `(월 1~10, 광 여부)` 정수 인코딩. 예: `월*10 + 순번`
- 화투패 이미지는 **Assets 시트에 도형 그룹**으로 제작(둥근 사각형 + 월별 문양/텍스트). 외부 이미지 파일 의존 없이 파일 하나로 배포 가능
- 실제 화투 이미지를 쓰고 싶으면 Assets 시트에 그림으로 붙여넣기만 하면 교체됨 (이름 규칙: `Card_01a`, `Card_01b`, …, `Card_Back`)

### 족보 판정 (`modHand`)
랭크 정수 하나로 비교 가능하게 설계:
```
광땡(38/18/13): 900번대
땡(1~10땡):     800 + 월
특수(알리~세륙): 700대 고정값
끗수(갑오~1끗):  100 + 끗
망통:            0
```

### 게임 흐름 (상태 머신)
```
IDLE → [게임 시작] → ANTE(기본 판돈 걷기) → DEAL(셔플·배분·애니메이션)
     → BET(선 플레이어부터 콜/하프/다이) → SHOWDOWN(패 공개)
     → SETTLE(승자에게 판돈, 기록 저장) → IDLE
```
- 다이 시 즉시 SETTLE
- 파산 시 게임 오버 화면 + 재시작

---

## 4. AI 설계 (생성형 AI 불필요, 순수 VBA)

입력 4가지로 확률적 판단:
1. **핸드 강도** — 족보 랭크를 0~1로 정규화한 테이블 (Data 시트)
2. **판돈 대비 콜 비용** — 콜 비용이 클수록 요구 강도 상승
3. **자기 자금 비율** — 자금이 적으면 보수적
4. **성향 파라미터** — 공격성, 블러핑 확률, 다이 임계값 (난이도별 프리셋)

의사결정: `판단점수 = 핸드강도 + 성향보정 + Rnd()*변동폭` → 임계값 구간에 따라 하프/콜/다이. 블러핑은 낮은 강도에서도 `블러핑확률`로 하프 선택 → 매판 다른 플레이.

난이도 프리셋:
- **초급**: 핸드 강도만 보고 판단, 블러핑 없음
- **중급**: 판돈·자금 고려, 가끔 블러핑
- **고급**: 플레이어의 최근 베팅 기록(하프 빈도, 다이 빈도)을 Save 시트에 누적해 임계값 동적 조정 + 블러핑

---

## 5. 게임형 UI 처리

- `Workbook_Open`: 격자선·머리글·수식입력줄 숨김, 전체화면 유사 모드, Game 시트 보호(도형 클릭만 허용)
- `Workbook_BeforeClose`: 엑셀 설정 원복
- 버튼: 도형 + `OnAction` 매크로 (콜/하프/다이/시작/저장/이어하기)
- 애니메이션: 카드 도형의 `Left/Top`을 루프로 보간 이동 + `DoEvents` (배분·패 공개 연출)
- 효과음: 카드 소리, 칩 소리, 승리/패배 — `.wav`를 Data 시트에 경로 저장하거나 시스템음 사용
- 화면 전환: 타이틀 → 게임 → 결과, 도형 그룹 표시/숨김으로 구현

---

## 6. 저장 / 이어하기

- Save 시트에 기록: 보유 자금, 진행 중 판 상태(패·판돈·턴), 전적(승/패/최대 판돈), AI 학습용 베팅 히스토리
- "이어하기" 버튼: Save 시트 읽어 `clsGameState` 복원
- 통합 문서 저장 시 자동 반영 (xlsm 자체가 저장소)

---

## 7. 제작 순서 (마일스톤)

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| **M1** | Game 시트 레이아웃 + Assets 패 도형 20장 + 버튼 | 게임판이 그럴듯하게 보임 |
| **M2** | 덱/셔플/배분 + 족보 판정 + 베팅 없이 즉시 쇼다운 | 승패가 정확히 판정됨 |
| **M3** | 콜/하프/다이 베팅 + 자금 정산 + 파산 처리 | 포인트가 정확히 오감 |
| **M4** | AI 판단 로직 + 난이도 3종 | AI가 다이·블러핑을 함 |
| **M5** | 배분/공개 애니메이션 + 효과음 + 타이틀/결과 화면 | 게임 느낌이 남 |
| **M6** | 저장/이어하기 + 게임 기록 + UI 다듬기 | v1 완성 |
| **v2** | 따당, 구사, 암행어사, 땡잡이 등 특수 규칙 | — |

---

## 8. 기술적 주의사항

- 매크로 보안: 사용자가 처음 열 때 "콘텐츠 사용" 필요 → 타이틀 화면에 안내 문구
- 64비트 오피스 대응: `Declare PtrSafe` 사용
- `Randomize`를 게임 시작 시 1회 호출 (셔플 편향 방지)
- 애니메이션 중 `Application.ScreenUpdating`/`EnableEvents` 상태 관리, 오류 시 원복하는 에러 핸들러 필수
- 도형 이름 규칙 통일 (`btn_Call`, `card_P1`, `card_AI1` …) — modUI가 이름으로 제어
