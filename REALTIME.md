# SHEEET 실시간화 전략 — "셀을 포기한다"는 것의 의미

> 게임 동작을 최대한 실시간으로 만들기 위한 아키텍처 검토 문서

---

## 1. 지금 왜 실시간이 안 되는가 (병목 = 셀 렌더링 엔진)

현재 SHEEET의 실시간 병목은 **셀을 게임 화면으로 쓰는 구조** 그 자체다.

```
플레이어 A가 셀 편집
  → onEdit 트리거가 구글 서버에서 실행 (비동기, 큐 대기)
  → LockService로 잠금 → 상태 계산 → 셀에 다시 쓰기
  → 플레이어 B의 브라우저가 그 셀 변경을 동기화 (푸시 없음)
= 왕복 1~3초 + Apps Script 실행 할당량 제한
```

즉 **셀을 게임 화면으로 쓰는 순간** onEdit 폴링 구조에 묶여 실시간이 불가능하다.
(IDEA.md에도 "폴링 지연을 설계 조건으로 받아들여 턴제에 최적화"라고 이 한계를 인정해 둠)

**"셀을 포기한다" = 셀 그리드를 게임 화면으로 쓰는 걸 버리고, 시트 안에 띄운 웹앱(HTML)에서 게임을 그린다는 뜻.**

---

## 2. 실시간으로 만드는 3가지 방법 (빠른 순)

| 방법 | 구조 | 지연 | 실시간성 |
|---|---|---|---|
| **① Firebase Realtime DB** ⭐ | HTML 안 JS ↔ Firebase (WebSocket) | ~50–100ms | 진짜 실시간 |
| ② HtmlService + `google.script.run` 폴링 | HTML ↔ Apps Script 왕복 | ~0.5–2초 | 어중간 |
| ③ 네이티브 셀 편집 동기화 | 지금 방식 | 1~3초 | 턴제만 가능 |

**핵심은 ①번:** 게임 UI를 `HtmlService`(구글 시트 안에 뜨는 HTML 창)로 만들고,
그 안의 클라이언트 자바스크립트에서 **Firebase Realtime Database**에 직접 연결한다.
Firebase는 WebSocket 기반이라 Apps Script 서버를 안 거치고 플레이어끼리 즉시 동기화된다.
Apps Script는 방 만들기·인증 같은 초기 세팅만 담당한다.

```
[구글 시트]  ← 로비 / 관전 / 업무위장 모드 (셀 그대로 활용)
    │  버튼 클릭 → HTML 게임창 열림
    ▼
[HtmlService 게임창] ←── WebSocket ──→ [Firebase Realtime DB]
   (클라이언트 JS로 렌더링)         (모든 플레이어 상태 즉시 공유)
```

---

## 3. ⚠️ 트레이드오프 — 독창성 리스크

SHEEET의 **차별점이자 심사 포인트**가 "셀=픽셀, 스프레드시트가 곧 게임판"이라는 비주얼 반전이다.
셀 렌더링을 완전히 버리면 → 그냥 "구글 시트 안에 띄운 웹게임"이 되어
**독창성(70점 배점의 핵심)이 약해질 위험**이 있다.

---

## 4. 추천 방향 — 하이브리드 (전부 버리지 않기)

- **셀(시트)** = 로비 + **관전 모드** + **업무 위장(Boss Key)**
  → 비주얼 개그와 "이거 스프레드시트임" 임팩트는 여기서 살림
  → 실시간 게임 상태를 1~2초마다 셀에 미러링만 함
- **HTML 게임창 + Firebase** = 실제 플레이어들의 실시간 대전 → 반응성 확보

→ **"링크 열면 시트가 게임방"이라는 컨셉과 실시간 반응성을 둘 다** 챙길 수 있다.

---

## 5. 선택지 정리

| 옵션 | 내용 | 장단점 |
|---|---|---|
| **A. 하이브리드 (추천)** | 셀은 로비/관전/보스키용, 실전은 HTML+Firebase 실시간 | 컨셉 + 실시간 둘 다 확보 |
| B. 완전 실시간 | 시트는 런처만, 게임은 전부 HTML+Firebase | 실시간 최강 / 독창성↓ 리스크 |
| C. 셀 유지 + 최적화 | 실시간 포기, 턴제로 가되 지연 최소화 | 컨셉 최강 / 반응성 한계 |

---

## 다음 단계

방향 확정 후 **오목 실시간 프로토타입**부터 구현.
(Firebase 무료 티어로 충분, 세팅 지원 가능)
