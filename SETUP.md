# SHEEET 개발·설치 가이드

> 팀원용 (오정빈 보라고 만든 문서). 이대로 따라하면 로컬에서 전체 파이프라인이 돌아간다.
> 궁금한 건 해찬한테.
> 프로덕션 주소: https://sheeet-zeta.vercel.app

---

## 0. 전체 구조 — 3분 이해

```
[Next.js 랜딩 (web/)]                      ← 유저가 게임 고르는 곳
        │  POST /api/room {game, players, ...}
        ▼
[Apps Script 웹 앱 (apps-script/room-factory.gs)]   ← "방 공장"
        │  게임별로 플레이어 수만큼 구글 시트 파일 생성
        │  + "링크 있는 모든 사용자 편집 가능" 권한
        │  + 설치형 onEdit 트리거 등록
        ▼
[플레이어별 시트 파일 = 게임판]              ← 참가자는 로그인 없이 플레이
```

핵심 아이디어 두 개:

1. **1인 1링크 = 신원.** 익명 사용자는 누가 편집했는지 식별이 불가능하다(구글 한계).
   그래서 플레이어마다 전용 파일을 만들고, 링크를 가진 사람 = 그 플레이어로 취급한다.
   흑 링크가 없으면 흑 돌을 놓을 방법 자체가 없다 → 턴 완전 강제.
2. **게임판 파일에는 코드가 없다.** 모든 로직은 호스트 계정의 중앙 Apps Script
   프로젝트 하나에서 돈다. 코드를 고치면 이후 만드는 모든 방에 적용된다.

---

## 1. 최초 설치 (호스트 1회, 10분)

팀에서 **한 명(호스트)의 구글 계정**으로만 하면 된다. 지금 호스트는 해찬 계정.
새로 자기 걸로 세팅하고 싶을 때만 아래를 따라 하면 됨.

1. [sheets.new](https://sheets.new) 로 새 스프레드시트 생성 (이게 "마스터 시트" — 관리용)
2. 메뉴 **확장 프로그램 → Apps Script** 클릭 → 편집기 열림
3. 기본 코드(`function myFunction...`) 전부 삭제
4. 이 레포의 **`apps-script/room-factory.gs` 내용 전체를 복사해서 붙여넣기**
5. **Cmd+S 저장** (프로젝트 이름은 아무거나, 예: SHEEET)
6. 상단 함수 드롭다운에서 **`testCreateOmokRoom`** 선택 → **실행**
   - "승인 필요" 팝업 → 계정 선택 → **"확인되지 않은 앱"** 경고가 떠도 정상
     (방금 본인이 붙여넣은 스크립트라 구글 검증을 안 받았을 뿐)
   - **고급 → "SHEEET(안전하지 않음)으로 이동" → 허용**
   - 시트/드라이브 권한을 요구하는 이유: 방 파일 생성 + 공유 설정 + 트리거 등록
7. 실행이 끝나면 하단 로그에 오목 방 링크 2개가 찍힌다 → 엔진 정상

## 2. 웹 앱 배포 (API 열기)

1. 편집기 오른쪽 위 파란 **배포 → 새 배포**
2. 톱니바퀴 → 유형 **"웹 앱"**
3. 설정: **실행 계정 = 나** / **액세스 권한 = 모든 사용자**
4. **배포** → 나온 URL(`https://script.google.com/macros/s/…/exec`)이 **팩토리 URL**
5. 브라우저에서 `팩토리URL?game=omok&players=2` 열어보기
   → 방 링크 JSON이 나오면 성공

## 3. ⚠️ 코드 수정할 때 — 이제 clasp 자동 배포다

예전처럼 편집기에 손으로 붙여넣지 않는다. 레포의 **`apps-script/` 폴더 자체가
clasp 프로젝트**다 (`.clasp.json`에 스크립트 ID가 연결돼 있고,
`.claspignore`가 프로토타입 `omok.gs` / `omok-duel.gs`를 배포에서 제외한다).
로컬에서 코드 고치고 명령어 두 줄이면 프로덕션에 반영된다.

### 사전 조건 (1회만)

```bash
npm i -g @google/clasp
clasp login          # 호스트 구글 계정으로 로그인
```

그리고 https://script.google.com/home/usersettings 에서 **Apps Script API 켜기**
(안 켜면 `clasp push`가 권한 에러를 뱉는다).

### 배포 절차

```bash
cd apps-script
clasp push -f        # 로컬 코드를 스크립트에 반영

# 웹 앱 새 버전 배포 (URL 유지)
clasp redeploy AKfycbxUsB0bpUaoCk0g_XciIoHQkV5xHXJK3bgGOLHq-9PHebq6i1s6q3EGCA1ZLaV_ctBvPw -d "설명"
```

- `clasp push -f` = 편집기에 붙여넣고 저장한 것과 같음 (트리거 로직은 바로 반영)
- `clasp redeploy …` = 옛날 "배포 관리 → 새 버전"과 같음. **웹 앱 URL은 그대로 유지**되고
  방 생성·판 그리기 코드만 새 버전으로 바뀐다. `-d` 뒤엔 이번 변경 설명을 짧게.

### 검증

```bash
curl -sL <웹앱URL>
# → {"version":"..."} 로 지금 배포된 버전이 찍힌다
```

버전 문자열이 방금 올린 것과 맞으면 성공.

**주의:** 이미 만들어진 방 파일들은 안 바뀐다 (옛 코드가 그린 판). **테스트는 반드시 새 방으로.**

## 4. 프론트엔드 로컬 실행

```bash
cd web
npm install
```

`web/.env.local` 파일 생성 (git에 안 올라감):

```
FACTORY_URL=https://script.google.com/macros/s/…본인의_웹앱_URL…/exec
```

```bash
npm run dev   # → http://localhost:3000
```

게임 카드 클릭 → 방 만들기 → 링크/QR 나오면 전체 파이프라인 성공.
프로덕션은 Vercel: https://sheeet-zeta.vercel.app (해찬 계정, main 브랜치 기준)

## 5. 게임별 사용법

### 오목 (2인)
- 방 만들면 흑/백 링크 2개. **각자 자기 링크만** 받아야 한다 (링크 = 신원)
- 빈 칸에 아무 글자나 입력 → 자기 차례 돌이 놓임
- 자기 차례 아닐 때 두면 → 판 전체 빨간 ❌ 플래시 + 입력 삭제
- 닉네임 칸에 이름 입력 → "👉 ⚫ 정빈 님의 차례입니다" 로 표시
- 5목 완성 → 금색 스윕 연출 + 승리 배너. "새 게임" 체크박스로 리셋

### 픽셀 기억 그리기 (2~6인)
- 프론트에서 인원/라운드(1~5)/난이도(8×8·3색 / 10×10·4색 / 12×12·5색) 선택
- 흐름: 아무나 [라운드 시작] 체크 →
  **10초 정답 암기 → 60초 그리기 → 20초 전원 그림 감상 → 정답 공개 + 채점**
- 색칠 방법 3가지 (뭘 쓰든 인정):
  1. 팔레트 견본 칸 복사(Cmd+C) → 원하는 칸들 선택 → 붙여넣기(Cmd+V) ← 제일 빠름
  2. 툴바 채우기(🖌 페인트통)로 직접 칠하기
  3. 셀에 숫자 1~5 입력 (1=빨강 2=파랑 3=노랑 4=초록 5=보라)
- 채점: 그려진 픽셀 기준 일치율(%). 빈 판은 0%. 라운드 1등 +1점, 최종 우승자 발표
- 검정 액자 **안쪽**이 캔버스다. 액자 밖에 그리면 무시됨

### 경마 (인원 제한 없음, 링크 1개 공유)
- 다른 게임과 달리 링크가 **하나**다. 참가자 전원이 같은 링크로 입장한다.
- 베팅 표에 자기 닉네임을 쓰고, 그 옆에 걸 말 번호(**1~5**)를 적는다
- 준비되면 **아무나 [레이스 시작] 체크** → 말 5마리가 랜덤으로 달리는 애니메이션이 돈다
- 우승한 말에 건 사람들이 승리. 링크가 하나뿐이라 신원 강제는 없음(닉네임 신뢰 기반)

### 라이어 게임 (3~8인, 1인 1링크)
- 방 만들면 인원수만큼 링크. **각자 자기 링크만** 받는다
- 시작하면 각자 파일에 제시어가 뜨는데, **딱 한 명만 "당신이 라이어"** 를 본다
  (라이어는 제시어를 모른 채 아는 척해야 한다)
- 말로 토론 → 각자 자기 파일의 투표 칸에 의심 가는 플레이어 번호를 입력
- 전원 투표가 끝나면 자동으로 결과 공개 (라이어가 지목됐는지)

### 3D 미로 탈출 (2인, 1인 1링크)
- 방 만들면 링크 2개. **각자 자기 링크만** 받는다
- 각 파일에 **1인칭 3D 시점이 시트 셀로 렌더링**된다 (벽·통로가 원근감 있게 그려짐)
- 이동 칸에 **w/a/s/d** 입력: `w` 전진 · `a`/`d` 좌우 회전 · `s` 후진
- 둘이 같은 미로를 각자 헤매다가, **먼저 출구에 도달하는 사람이 승리**

### 퀴즈쇼 (인원 제한 없음, 링크 1개 공유 · 구글 슬라이드)
- 방을 만들면 **슬라이드 링크 하나**가 나온다. 전원이 같은 링크로 입장
- 호스트(구글 로그인 필요)가 메뉴 [🧠 퀴즈쇼] → [컨트롤 열기] → [게임 시작]
  — 복제된 방에서 첫 실행 시 권한 승인 창이 한 번 뜬다
- 참가자는 대기석 아바타를 게임장으로 끌어온 뒤, 카테고리 투표 → 문제마다
  정답이라 생각하는 보기 카드 위에 아바타를 올린다 (4초 유지 시 답 확정)
- 정답 +10점, 누적 점수로 최종 순위

### 운명의 문 (인원 제한 없음, 링크 1개 공유 · 구글 슬라이드)
- 퀴즈쇼와 같은 방식 — 링크 하나, 호스트가 메뉴에서 시작
- 매 라운드 아바타를 문 위로 끌어 선택 → 생존/탈락, 끝까지 살아남으면 승리

## 6. 새 게임 추가하는 법 (개발자용)

`room-factory.gs`의 `GAMES` 레지스트리에 등록하면 끝:

```js
const GAMES = {
  mygame: {
    name: '내 게임',
    minPlayers: 2,
    maxPlayers: 4,
    roleLabel: i => '플레이어 ' + (i + 1),  // 링크에 붙는 역할 이름
    build: buildMyGameRoom,   // (roomId, n, opts) → {fileIds, urls, ...상태}
    onEdit: handleMyGameEdit, // (e, room, roomId) → 편집 이벤트 처리
  },
};
```

- `build`: 플레이어별 시트 파일을 만들고 판을 그린 뒤 방 상태 객체를 반환
- `onEdit`: 어느 방의 어느 판에서든 편집이 오면 라우팅돼서 호출됨.
  방 상태(room)를 수정하면 자동 저장된다.
  **함수를 반환하면 잠금 해제 후 실행**된다 (픽셀 게임의 라운드 연출처럼 오래 걸리는 작업용)
- 프론트: `web/app/page.js`의 `GAMES` 배열에 카드 하나 추가

## 7. 제약·트러블슈팅

| 증상/제약 | 원인·대응 |
|---|---|
| 방을 많이 만들면 옛 방이 멈춤 | 구글 트리거 한도(스크립트당 20개). 가장 오래된 방부터 자동으로 닫힌다(파일은 남음). 정상 동작 |
| 익명 손님 입력에 반응 없음 | 단순 트리거는 익명 편집에 안 돈다. 설치형 트리거(`onMove`)가 등록됐는지 확인 — 편집기 왼쪽 시계 아이콘 |
| 새 방 판이 옛날 모양 | `clasp redeploy` 안 함. §3 참고 |
| 붙여넣기 후 셀 테두리 사라짐 | 원래 그런 것(붙여넣기가 서식까지 복사). 다음 단계 전환 때 자동 재도색됨 |
| 입력→반영 지연 | 1~3초가 정상. 시트 동기화의 물리적 한계고, 그래서 전 게임이 턴제다 |
| 라운드 중 닉네임 바꿈 | 라운드 끝날 때 반영될 수 있음 (알려진 제약) |
| 방 링크를 잘못 바꿔 받음 | 흑/백이 뒤바뀔 뿐 게임은 됨. 픽셀은 상관없음 |

## 8. 파일 안내

```
apps-script/
  room-factory.gs         ← 실제 운영 코드 (clasp로 배포되는 대상)
  omok.gs                 ← 초기 프로토타입 1: 한 파일 오목 (기록용, .claspignore로 배포 제외)
  omok-duel.gs            ← 초기 프로토타입 2: 1인 1링크 오목 단독 (기록용, .claspignore로 배포 제외)
  slides-doors.gs         ← 구글 슬라이드 게임 "운명의 문" (템플릿에 설치, .claspignore로 공장 배포 제외)
  slides-doors-sidebar.html ← 위 게임의 사이드바 UI
  slides-quiz.gs          ← 구글 슬라이드 게임 "퀴즈쇼" (템플릿에 설치, .claspignore로 공장 배포 제외)
  slides-quiz-sidebar.html ← 위 게임의 사이드바 UI
web/                      ← Next.js 랜딩 (게임 선택 → 방 생성 → 링크/QR)
IDEA.md                   ← 기획서
```

## 9. 슬라이드 게임 템플릿 등록 (퀴즈쇼·운명의 문, 호스트 1회)

슬라이드 게임은 시트 게임과 달리 공장이 판을 그리지 않는다.
**스크립트가 설치된 템플릿 프레젠테이션을 복제**하는 방식이라, 게임당 템플릿 1개를 만들어 등록해야 한다.

1. **템플릿 만들기** (게임당 1회)
   - 새 프레젠테이션 → 확장 프로그램 → Apps Script
   - `Code.gs`에 `slides-quiz.gs` 내용 붙여넣기
   - 파일 추가(+) → HTML → 이름을 정확히 `slides-quiz-sidebar` 로 생성, 같은 이름의 html 내용 붙여넣기 → 저장
   - (선택) 드라이브 "운명의문_아바타" 폴더에 12지신 PNG 12장 업로드 (`12지신_캐릭터_파일/` 참조)
   - 프레젠테이션 새로고침 → 메뉴 [🧠 퀴즈쇼] → [보드 생성] 까지 실행해 둔다
     (복제본이 바로 게임 가능한 상태가 되도록)
   - 운명의 문도 같은 방법 (`slides-doors.gs` / `slides-doors-sidebar`)
2. **공장에 템플릿 ID 등록**
   - 템플릿 URL의 `/d/`와 `/edit` 사이 문자열이 파일 ID
   - 공장 스크립트 편집기 → 프로젝트 설정(⚙️) → 스크립트 속성에 추가:
     - `QUIZ_TEMPLATE_ID` = 퀴즈쇼 템플릿 파일 ID
     - `DOORS_TEMPLATE_ID` = 운명의 문 템플릿 파일 ID
3. **주의**
   - 템플릿은 공장을 배포한 계정의 드라이브에 있어야 한다 (복제 권한)
   - 방마다 새 복제본이 생기므로, 호스트는 복제된 방에서 처음 메뉴를 누를 때 권한 승인을 한 번 해야 한다
